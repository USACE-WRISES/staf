"""StreamCurves domain package.

Pure computation/IO layer ported from the R app's ``R/`` pipeline and
computational helpers. No ``shiny`` imports allowed anywhere in this package —
everything here is importable and testable headlessly (pytest).
"""
