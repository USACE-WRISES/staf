# EASI — runtime dependencies, pinned for reproducible deploys.
#
# Posit Connect Cloud installs from this file (pip only — no apt/system libs;
# every package below ships manylinux wheels for Python 3.12). NO API keys are
# required at runtime. Dev/test tools live in requirements-dev.txt.

# --- App framework + interactive map ---
shiny==1.6.3
shinywidgets==0.8.1
ipyleaflet==0.20.0
anyio==4.13.0

# --- HyRiver geospatial/hydrology stack (matched 0.19.x set) ---
pynhd==0.19.4              # NLDI snap/navigate/split_catchment, WaterData, VAA, StreamCat
py3dep==0.19.0             # USGS 3DEP DEM (cross-sections)
pygeohydro==0.19.4         # NLCD, WQP, NID helpers
pygeoutils==0.19.5
pygeoogc==0.19.4
async-retriever==0.19.3
hydrosignatures==0.19.3

# --- Geospatial core (binary wheels bundle GDAL/GEOS/PROJ) ---
geopandas==1.1.3
pyogrio==0.12.1            # GeoJSON I/O engine (reads data/physio_divisions.geojson)
shapely==2.1.2
pyproj==3.7.2
rasterio==1.5.0
rioxarray==0.22.0
xarray==2026.4.0
numpy==2.4.6
pandas==3.0.3

# --- Cross-section plot + PDF export (headless: matplotlib Agg, reportlab) ---
matplotlib==3.11.0
reportlab==4.5.1
plotly==6.8.0             # interactive cross-section plot (zoom/pan) via shinywidgets

# --- HTTP (keyless national data services) ---
requests==2.34.2
