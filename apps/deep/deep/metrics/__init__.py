"""DEEP desktop metric auto-compute (Phase 3).

Reuses EASI's datasource + geomorphology code to compute the raw value for the
desktop-derivable detailed metrics, keyed by their STAF metricId, so DEEP can
prefill the Measure worksheet. See :mod:`deep.metrics.computed`.
"""
