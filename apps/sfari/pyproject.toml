[project]
name = "sfari"
version = "0.0.1"
description = "Stream Functional Assessment Rapid Index (SFARI) web app"
requires-python = ">=3.12"

[tool.pytest.ini_options]
pythonpath = ["."]
testpaths = ["tests"]
addopts = "-q"
